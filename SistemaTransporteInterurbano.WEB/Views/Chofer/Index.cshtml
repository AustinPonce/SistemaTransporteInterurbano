@model List<SistemaTransporteInterurbano.Models.Entities.Chofer>

@{
    ViewData["Title"] = "Gestión de Choferes";
    Layout = null;
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Sistema de Transporte Interurbano</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/glass.css" />
</head>
<body>

    <nav class="navbar glass-nav px-4 py-3">
        <span class="navbar-brand text-white fw-bold fs-5">
            <i class="bi bi-bus-front me-2 text-info"></i>Sistema de Transporte Interurbano
        </span>
        <a asp-controller="Autenticacion" asp-action="CerrarSesion" class="btn btn-cerrar btn-sm">
            <i class="bi bi-box-arrow-right me-1"></i>Cerrar Sesión
        </a>
    </nav>

    <div class="container py-5">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <a asp-controller="Home" asp-action="Index" class="btn btn-outline-light btn-sm rounded-pill px-3 me-3">
                    <i class="bi bi-arrow-left me-1"></i>Inicio
                </a>
                <span class="text-white fw-bold fs-4">
                    <i class="bi bi-person-badge me-2 text-info"></i>Choferes
                </span>
            </div>
            <a asp-action="Agregar" class="btn btn-gradient-add">
                <i class="bi bi-plus-circle me-1"></i>Agregar Chofer
            </a>
        </div>
            @if (TempData["MensajeError"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show border-0 rounded-4 bg-danger text-white mb-4" role="alert">
                    <i class="bi bi-exclamation-circle-fill me-2"></i>@TempData["MensajeError"]
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
                </div>
            }

        @if (TempData["MensajeExito"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show border-0 rounded-4 bg-success text-white mb-4" role="alert">
                <i class="bi bi-check-circle-fill me-2"></i>@TempData["MensajeExito"]
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
            </div>
        }

        <div class="glass-card p-4 mb-4">
            <form asp-action="Index" method="get" class="d-flex gap-2">
                <div class="input-group glass-input-group flex-grow-1">
                    <span class="input-group-text"><i class="bi bi-search"></i></span>
                    <input type="text" name="filtroNombre" class="form-control"
                           placeholder="Buscar por nombre o apellido..."
                           value="@ViewBag.FiltroNombre" />
                </div>
                <button type="submit" class="btn btn-gradient-search px-4">Buscar</button>
                @if (!string.IsNullOrEmpty(ViewBag.FiltroNombre as string))
                {
                    <a asp-action="Index" class="btn btn-outline-light px-3 rounded-pill">
                        <i class="bi bi-x-lg"></i>
                    </a>
                }
            </form>
        </div>

        <div class="glass-card p-0 overflow-hidden">
            @if (!Model.Any())
            {
                <div class="text-center text-white-50 py-5">
                    <i class="bi bi-people fs-1 d-block mb-2"></i>
                    No hay choferes registrados.
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-glass mb-0">
                        <thead>
                            <tr>
                                <th>Identificación</th>
                                <th>Nombre</th>
                                <th>Apellidos</th>
                                <th class="text-end">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var chofer in Model)
                            {
                                <tr>
                                    <td>@chofer.Identificacion</td>
                                    <td>@chofer.Nombre</td>
                                    <td>@chofer.Apellidos</td>
                                    <td class="text-end">
                                        <a asp-action="Editar" asp-route-id="@chofer.ChoferId"
                                           class="btn btn-sm btn-outline-info rounded-pill px-3">
                                            <i class="bi bi-pencil me-1"></i>Editar
                                        </a>
                                        <form asp-action="Eliminar" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@chofer.ChoferId" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill px-3 ms-2" onclick="return confirm('¿Está seguro que desea eliminar este chofer? Esta acción no se permite si el chofer tiene viajes registrados.')">
                                                <i class="bi bi-trash me-1"></i>Eliminar
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>

    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>